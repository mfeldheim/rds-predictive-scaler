name: Build and Push Multi-Arch Docker Image

on:
  push:
    tags:
      - '*'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.7

      - name: Set up Go
        uses: actions/setup-go@v5.1.0
        with:
          go-version: '1.25'
          cache: true
          cache-dependency-path: go.sum

      - name: Run Go tests
        run: go test ./... -v

  buildx:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.7

      - name: Generate tag
        id: generate-tag
        run: |
          TAG_NAME="${{ github.ref_name }}"
          TAG_NAME="${TAG_NAME//\//-}"
          echo "tag=${TAG_NAME}" >> $GITHUB_OUTPUT

      - name: Determine if should push
        id: should-push
        run: |
          if [[ "${{ github.ref_type }}" == "tag" || "${{ github.ref }}" == "refs/heads/main" ]]; then
            echo "push=true" >> $GITHUB_OUTPUT
          else
            echo "push=false" >> $GITHUB_OUTPUT
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3.6.0

      - name: Login to GitHub Container Registry
        if: steps.should-push.outputs.push == 'true'
        uses: docker/login-action@v3.1.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build image (no push)
        if: steps.should-push.outputs.push == 'false'
        uses: docker/build-push-action@v6.2.0
        with:
          context: .
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64,linux/arm64
          push: false

      - name: Build, tag, and push image to GHCR
        if: steps.should-push.outputs.push == 'true'
        uses: docker/build-push-action@v6.2.0
        id: build-image
        with:
          context: .
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ghcr.io/mfeldheim/rds-predictive-scaler:latest,ghcr.io/mfeldheim/rds-predictive-scaler:${{ steps.generate-tag.outputs.tag }}
